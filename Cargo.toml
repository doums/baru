[package]
name = "baru"
version = "0.2.3"
authors = ["pierre <dommerc.pierre@gmail.com>"]
edition = "2018"
links = "netlink,audio"
build = "build.rs"

[dependencies]
chrono= "0.4"
regex = "1"
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.8"

[build-dependencies]
cmake = "0.1"
